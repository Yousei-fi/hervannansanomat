<!DOCTYPE html>
<html {{ site.language_attributes }}>
<head>
    <meta charset="{{ site.charset }}">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="profile" href="http://gmpg.org/xfn/11">
    {{ function('wp_head') }}
</head>
<body class="{{ body_class }} bg-brand-light text-ink">
    {{ function('wp_body_open') }}
    <a href="#main" class="sr-only focus:not-sr-only focus:absolute focus:left-6 focus:top-6 focus:rounded-full focus:bg-white focus:px-4 focus:py-2 focus:text-brand-dark focus:shadow-soft">Ohita sisältöön</a>

    <div class="min-h-screen flex flex-col">
        <header class="sticky top-0 z-50 border-b border-black/5 bg-white/90 backdrop-blur">
            <div class="container flex items-center justify-between gap-4 py-4">
                <div class="flex items-center gap-3">
                    <a href="{{ site.url }}" class="flex items-center gap-3">
                        {% if theme_logo %}
                            <img src="{{ theme_logo }}" alt="{{ site.name }}" class="h-10 w-auto">
                        {% elseif function('has_custom_logo') %}
                            {{ function('the_custom_logo') }}
                        {% else %}
                            <span class="text-xl font-serif font-semibold text-brand-dark">{{ site.name }}</span>
                        {% endif %}
                    </a>
                    <span class="hidden md:inline-flex items-center rounded-full bg-brand-sand px-3 py-1 text-xs font-semibold uppercase tracking-[0.14em] text-brand-dark">Paikallinen vuodesta 1975</span>
                </div>

                <div class="flex items-center gap-3">
                    <nav class="hidden md:flex items-center gap-6" aria-label="Päävalikko">
                        <a class="nav-link" href="{{ site.url }}">Etusivu</a>
                        <div class="relative group">
                            <button class="nav-link inline-flex items-center gap-1" type="button">
                                Uutiset
                                <svg class="h-4 w-4" viewBox="0 0 20 20" fill="currentColor">
                                    <path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 011.06.02L10 10.94l3.71-3.71a.75.75 0 111.06 1.06l-4.24 4.24a.75.75 0 01-1.06 0L5.21 8.29a.75.75 0 01.02-1.08z" clip-rule="evenodd" />
                                </svg>
                            </button>
                            <div class="absolute left-0 mt-3 hidden min-w-[240px] rounded-2xl bg-white p-4 shadow-card ring-1 ring-black/5 group-hover:block">
                                {% if categories %}
                                    <ul class="space-y-2">
                                        {% for cat in categories %}
                                            <li><a class="nav-link" href="{{ cat.link }}">{{ cat.name }}</a></li>
                                        {% endfor %}
                                    </ul>
                                {% else %}
                                    <p class="text-sm text-gray-600">Ei kategorioita.</p>
                                {% endif %}
                            </div>
                        </div>
                        <a class="nav-link" href="{{ site.url }}/nakolehti">Näköislehti</a>
                        <a class="btn-primary text-sm px-4 py-2" href="{{ site.url }}/mainosta">Mainosta!</a>
                    </nav>
                    <button type="button" class="btn-ghost md:hidden" aria-expanded="false" aria-controls="mobile-nav" data-nav-toggle>
                        <span class="sr-only">Valikko</span>
                        <svg class="h-6 w-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path stroke-linecap="round" d="M4 7h16M4 12h16M4 17h16" />
                        </svg>
                    </button>
                </div>
            </div>
            <div id="mobile-nav" class="md:hidden border-t border-black/5 bg-white shadow-soft hidden" data-nav-panel>
                <div class="container py-4 flex flex-col gap-3">
                    <a class="nav-link py-2" href="{{ site.url }}">Etusivu</a>
                    <details class="group">
                        <summary class="nav-link py-2 flex items-center justify-between cursor-pointer">
                            Uutiset
                            <svg class="h-4 w-4" viewBox="0 0 20 20" fill="currentColor">
                                <path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 011.06.02L10 10.94l3.71-3.71a.75.75 0 111.06 1.06l-4.24 4.24a.75.75 0 01-1.06 0L5.21 8.29a.75.75 0 01.02-1.08z" clip-rule="evenodd" />
                            </svg>
                        </summary>
                        <div class="pl-3 flex flex-col gap-2 py-2">
                            {% if categories %}
                                {% for cat in categories %}
                                    <a class="nav-link" href="{{ cat.link }}">{{ cat.name }}</a>
                                {% endfor %}
                            {% else %}
                                <span class="text-sm text-gray-600">Ei kategorioita.</span>
                            {% endif %}
                        </div>
                    </details>
                    <a class="nav-link py-2" href="{{ site.url }}/nakolehti">Näköislehti</a>
                    <a class="btn-primary w-full justify-center" href="{{ site.url }}/mainosta">Mainosta!</a>
                </div>
            </div>
        </header>

        <main id="main" class="flex-1">
            {% block content %}{% endblock %}
        </main>

        <footer class="mt-12 border-t border-black/5 bg-white">
            <div class="container grid gap-10 py-12 md:grid-cols-3">
                <div class="space-y-3">
                    <p class="eyebrow">Hervannansanomat</p>
                    <p class="text-lg font-serif font-semibold text-brand-dark">Tulemme kotiisi joka keskiviikko.</p>
                    <p class="text-sm text-gray-600">Aidosti paikallinen jo vuodesta 1975. Hervannan, opiskelijoiden ja yritysten yhteisölehti.</p>
                </div>
                <div class="space-y-3">
                    <p class="eyebrow">Yhteystiedot</p>
                    <div class="text-sm text-gray-700 space-y-2">
                        <p>Pietilänkatu 1<br>33720 Tampere</p>
                        <p>Toimitus: <a class="underline" href="mailto:hs@hervannansanomat.fi">hs@hervannansanomat.fi</a><br>Ilmoitukset: <a class="underline" href="mailto:hsilmo@hervannansanomat.fi">hsilmo@hervannansanomat.fi</a></p>
                        <p>Jakelupalaute: 0400 859 766</p>
                    </div>
                </div>
                <div class="space-y-3">
                    <p class="eyebrow">Seuraa</p>
                    <div class="flex gap-3">
                        <a class="btn-ghost" href="https://www.facebook.com" target="_blank" rel="noreferrer">Facebook</a>
                        <a class="btn-ghost" href="https://www.instagram.com" target="_blank" rel="noreferrer">Instagram</a>
                    </div>
                    <p class="text-xs text-gray-500">Painos ja jakelu paikallisesti Hervannassa ja lähialueilla.</p>
                </div>
            </div>
        </footer>
    </div>
    {{ function('wp_footer') }}
</body>
</html>

